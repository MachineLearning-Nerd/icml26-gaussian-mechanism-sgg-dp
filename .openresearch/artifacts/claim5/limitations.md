# Paper Claim 5 limitations

Table 2 values are published to six decimals, so tangent residuals within
`1e-6` are treated as rounding-level equality. The exact residuals are
reported; no failed row is rounded into a pass.
