# Release audit method

The audit starts only from the candidate Space's `README.md`, `logbook.json`,
and `pages/index.md`. It follows the logbook tree and local links, checks every
required marker on every current claim page, independently hashes the protected
judged file set, validates the upload allowlist, and scans opened text for
credential patterns. No dashboard or OpenResearch-only evidence is consulted.
